#!/usr/bin/env node
/**
 * taleus-node CLI entry point.
 *
 * Starts the always-on trading service against a local cadre node. Argument
 * parsing, config loading, and lifecycle wiring land with
 * `feat-taleus-node-service`.
 */

function main(): void {
	console.error('taleus-node CLI not implemented yet — see feat-taleus-node-service')
	process.exitCode = 1
}

main()
